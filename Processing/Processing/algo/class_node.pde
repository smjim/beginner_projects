//calculate isovalue of configuration of the square to use lookup table
//node {a,b,c,d}=binary, a*8+b*4+c*2+d*1

class Node {
  int index;
  Boolean status;
  
  Node(Boolean state) {
    status = state;
    
  }
  
}
